<div class="item-container">
  <span class="page-header"><h1>Categories</h1></span>

<% @categories.each do |category| %>
  <div class="individual-item-index">
    <p><%= link_to category.name, category_path(category) %></p>
  </div>
<% end %>
</div>