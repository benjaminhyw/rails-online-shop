<h2><%= @item.name %></h2>

<% if !@item.image.blank? %>
  <img src="<%= @item.image %>">
<% else %>
  IMAGE NOT AVAILABLE
<%end%>

<ul>
  <li>Description: <%= @item.description %></li>
  <li>Quantity: <%= @item.quantity %></li>
  <li>Price: <%= @item.price %></li>
</ul>